# 老驱动 GPU 栈：驱动只支持到 CUDA 11.4 时，用这套（需要 Python 3.10）。
# torch 单独装（见 README_GPU.txt 的"老驱动 GPU 加速"一节）：
#   .venv\Scripts\python -m pip install torch==1.12.1+cu113 --index-url https://download.pytorch.org/whl/cu113
numpy==1.23.5
soundfile>=0.12
huggingface_hub==0.26.2
safetensors==0.4.5
