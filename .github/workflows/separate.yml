name: TIGER-DnR 分离

on:
  workflow_dispatch:
    inputs:
      input:
        description: "要分离的视频/音频在仓库里的路径（如 test/原片.mp4）"
        required: true
        default: "test/原片.mp4"
      chunk:
        description: "推理窗口秒数：12 / 20 / 30（越大上下文越多、越慢）"
        required: false
        default: "12"

jobs:
  separate:
    runs-on: ubuntu-latest
    # 公开仓库想用 GPU runner（几分钟跑完）：把上面一行换成 GitHub 官方文档
    # 里 GPU runner 的标签（如 ubuntu-24.04-gpu），私有仓库 GPU 是收费的。
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 安装 ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
          python -m pip install -r requirements_ci.txt

      - name: 校验 apply_dynamics.lua 语法
        run: |
          sudo apt-get install -y lua5.4
          luac5.4 -p scripts/apply_dynamics.lua

      - name: 运行分离（防休眠在云端不需要，脚本自动忽略）
        run: |
          python separate.py --input "${{ inputs.input }}" --chunk ${{ inputs.chunk }} --device auto

      - name: 响度/动态分析（对照原片对白）
        run: |
          python master.py --actors test

      - name: 排 Reaper 工程（非破坏式：响度在轨道音量，压缩参数写 dynamics）
        run: |
          python assemble_rpp.py --actors test

      - name: 上传分离结果
        uses: actions/upload-artifact@v4
        with:
          name: dubbing-output
          path: |
            work/separated/*.wav
            work/separated/*.json
            work/reaper/*.rpp
            work/reaper/*.json
            work/reaper/*.lua
          if-no-files-found: error
